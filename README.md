# Academia 产品原型

Academia 的 13 页可交互产品原型。

在线预览：https://anvilreason.github.io/academia/

## 本地预览

直接打开 `index.html`，或在项目目录运行：

```bash
python3 -m http.server 8080
```

然后访问 http://localhost:8080/。

## 发布

GitHub Pages 从 `main` 分支的仓库根目录发布。推送 `index.html` 的更新后，线上页面会自动刷新。
